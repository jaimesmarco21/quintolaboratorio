using System;
class program
{
    static void Main()
    {
        int i = 1; // Inicializamos i en 1.
        while (i <= 10) // Mientras i sea menor o igual a 10.
        {
            Console.WriteLine(i);
            i++; // Incrementamos i en 1.
        }
    }
}